# Calculate the sum of two numbers entered by the user.
# The program should prompt the user to enter the first number, then wait for them to press Enter before it asks for the second number.
a = input("Enter First Number : ")
b = input("Enter Second Number : ")
sum = int(a) + int(b)
print("Sum = ", sum)
